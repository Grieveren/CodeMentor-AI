{
  "name": "@codementor-ai/code-executor-client",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run generate && tsc",
    "generate": "node generate-proto.js",
    "clean": "rm -rf dist generated",
    "dev": "npm run build",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.10.0"
  },
  "devDependencies": {
    "grpc-tools": "^1.12.0",
    "ts-proto": "^1.176.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "files": [
    "dist",
    "generated",
    "README.md"
  ]
}

